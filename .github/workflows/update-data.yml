name: Update NPB Data

# 試合後の日本時間22:00〜翌1:00は毎時、加えて毎朝6:00頃に
# 自動でシーズン中の成績データを更新し、
# 変更があればコミット・pushする。pushされるとVercelが自動で再デプロイする。
#
# 過去年度(完成済みシーズン)のスクレイピング結果は .cache/raw に永続キャッシュし、
# 実行のたびに全年度を再取得しないようにしている。当年分のキャッシュだけ実行前に
# 破棄することで、シーズン中の最新試合結果だけを毎回取得し直す。

on:
  schedule:
    - cron: "0 13-16 * * *" # UTC 13:00〜16:00 = JST 22:00〜翌1:00、毎日
    - cron: "0 21 * * *" # UTC 21:00 = JST 6:00、毎日（予備更新）
  workflow_dispatch: {} # 手動実行用（GitHubのActionsタブから「Run workflow」で実行可能）

# 実行が遅延しても、データ更新処理を同時に走らせない。
concurrency:
  group: npb-data-update
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Restore scrape cache
        uses: actions/cache/restore@v4
        with:
          path: .cache/raw
          key: npb-scrape-cache-${{ github.run_id }}
          restore-keys: |
            npb-scrape-cache-

      - name: Invalidate current-year cache (force refetch of in-progress season)
        run: |
          YEAR=$(TZ=Asia/Tokyo date +%Y)
          rm -f ".cache/raw/${YEAR}-"*.html
          echo "Cleared cached pages for ${YEAR}"

      - run: npm ci

      - name: Rebuild current season data
        run: |
          YEAR=$(TZ=Asia/Tokyo date +%Y)
          BUILD_YEAR="$YEAR" npm run build-data

      - name: Enrich current-year player details
        run: |
          YEAR=$(TZ=Asia/Tokyo date +%Y)
          npx tsx scripts/build-player-bio.ts "$YEAR"
          npx tsx scripts/build-fielding-position-current.ts "$YEAR"
          # build-data内でも一度保存するが、その時点では年齢・選手IDの補完前。
          # 補完済みデータで当日分を上書きし、日次・月間比較の照合キーをそろえる。
          npx tsx scripts/snapshot-current-season.ts

      - name: Validate generated data
        run: npm run validate-data

      - name: Save scrape cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .cache/raw
          key: npb-scrape-cache-${{ github.run_id }}

      - name: Commit and push if data changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ public/search-index.json public/compare-index.json
          if git diff --staged --quiet; then
            echo "No data changes"
          else
            git commit -m "chore: NPBデータを自動更新 ($(TZ=Asia/Tokyo date +%Y-%m-%d))"
            git push
          fi
